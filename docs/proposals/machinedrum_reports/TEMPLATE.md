# WP-XX <title>: report

- **Status:** done / blocked / review
- **Branch and commit:** `<branch>` @ `<sha>`
- **Date:**
- **Agent:**

## What was done

Two to five sentences: what changed, and where (files, functions,
addresses).

## Acceptance check

The command you ran and its output, pasted, not summarised.

```
$ <command>
<output>
```

## Measured

Every new number, marked ✅ (measured), 🟡 or *inferred*, with the command
or file that produced it. Say which section of `MACHINEDRUM_MACHINE.md` it
went into.

## Retracted

Anything an earlier report or section 12 said that this packet showed to
be wrong, marked ❌, and where you corrected it.

## Open and handover

- What is not done, and why.
- Decisions the user has to make (name them D-something if new).
- What the next packet needs to know that is not in the code.
