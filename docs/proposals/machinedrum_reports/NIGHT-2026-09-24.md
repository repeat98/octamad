# Machinedrum overnight report — 24 September 2026

Branch `machinedrum`, pushed to `origin/machinedrum`; current HEAD is
`968c88c`. No hardware was used, nothing was flashed, and no Elektron-derived
byte, image, sample, `.syx`, snapshot or extracted blob was committed.

## Packet status

| Packet | Status | Completion commit | User action |
|---|---|---|---|
| WP-A1 memory ledger | blocked | `dea1167` | provide/fix a heavy slice+recorder fixture; both attempts exited `-11` before a wordmap |
| WP-A2 layout | review | `3573d25` | sign off stock-window sacrifices, six P-I cap, and voice-home A/B/C |
| WP-A3 relocation/driver | blocked | `cab97a3` | choose the A2 voice-home policy, then rerun the twelve-kit gate |
| WP-A4 relocated init | review | `cab97a3` | sign off the six-voice init only after A2/A3 |
| WP-A6 cycle report | blocked | `cab97a3` | choose D1 mix; investigate c40; measured mix cost is still absent |
| WP-B1 module skeleton | review | `f383ecc` | sign off the proposed resource envelope; B2 is not yet integrated |
| WP-B2 extraction audit | blocked | `50ecf58` / report `968c88c` | choose a packed code/table split or revise the shared-window owner map |
| WP-D1 held track key | done | `d4a626b` | none; stock fallback is FUNC + track |
| WP-R1 E12 delivery | review | `cdc9540` | D4: stream about 0.98M packed 24-bit words/s, or trade layout for a burst cache |
| WP-R2 TRX-S2 residual | review | `8513de2` | D3: current 36-word swap or full low-image swap at about 70 estimated cycles/sample |
| WP-R3 interpreter/JIT | blocked | `94ef689` | rerun after the user's WP-R4 vendor repin |
| WP-R5 c47_2 anomaly | done | `d27aec4` | none; track 7 EFM-CB is the heavy default voice |

WP-A5 remains blocked on D1; WP-D2/D5 and later packets were not started.

## Proposed-layout results

All A3, A4, A6 and B1 results are explicitly pending the user's layout
sign-off. The actual `X/Y:0x3400` driver gate is not qualified: c01_16,
c10, c10_3, c1d_16, c10_16, c20_16, c24_16 and c30_16 regress; c37_16 is
`33504/0`, and c47_2 retains `33415/87`. The successful A6 runs top out at
an inferred adjusted worst of `2,341.5` cycles/sample on cap5/c10_16, without
the unresolved D1 stereo mix; c40_16 reproduced `rc=139` twice.

WP-B2 confirms the proposed map cannot yet emit a payload: the pinned update
loads 29,760 words in `0x140000–0x147fff`, while the replay move maps that
span to the proposed sine allocation `0x30000–0x37fff`, which relocated init
writes in full. The source Y alias has 264 pre-boot words, 201 different from
the post-boot c10 P view. The audit therefore fails closed and writes no
payload or manifest.

## Decisions and handoff

- D1: choose simple level/pan mix (A) or the MD per-voice mix (B); B remains
  out of scope overnight and its cost is unmeasured.
- D3: retain the 36-word low-memory swap with the known c1d/c10 residuals, or
  pay about 70 estimated cycles/sample for the full low image (which still
  leaves c10 unresolved).
- D4: choose SDRAM-to-window streaming at the measured roughly 0.98M packed
  24-bit words/s, or sacrifice/relocate proposed window allocations for a
  bounded burst cache.
- A2/B2 layout: either implement the user-approved alias-aware packed split
  across `0x38000–0x3d9ff` plus Y-only tables, or reserve another 32K shared
  span and surrender a current owner. The existing post-hot budget is up to
  40,958 words against 40,960, leaving only two words before alignment.
- A1/R3: the heavy port fixture and the old `dsp_asm` `cmp a,b` encoding both
  remain external/toolchain work; no vendor rebuild was done.

The detailed measurements and retractions are in `MACHINEDRUM_MACHINE.md`
section 12 and the linked packet reports. The baseline remains
`md_replay cap4/c10: blocks: 33513 identical, 0 differ`.
